#include <bits/stdc++.h>
using namespace std;

int A, B;

int main() {
    cin >> A >> B;
    if (9 >= A && 9 >= B) {
        cout << A * B << endl;
    }
    else {
        cout << -1 << endl;
    }
}